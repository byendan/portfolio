<div class="project-form">
  <%= form_for(@tag_load, html: { multipart: true }, remote: true) do |f| %>
      <%= f.text_field :name, placeholder: "Tag name", class: "form-control" %>
      <%= f.text_area :content, placeholder: "Tag Description", rows: 15, class: "form-control" %>
      <%= f.file_field :image %>
      <h3>Current image</h3>
      <%= image_tag(@tag_load.image, size: "100x100") %>
    <div class="project-button">
      <%= f.submit "Post", class: "btn btn-primary "%>
    </div>
  <% end %>
</div>
